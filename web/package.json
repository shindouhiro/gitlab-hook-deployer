{
  "name": "auto-web-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -C apps/ui dev",
    "build": "pnpm -r build"
  },
  "pnpm": {
    "catalogs": {
      "default": {
        "vue": "^3.4.0",
        "@vueuse/core": "^10.9.0",
        "tailwindcss": "^3.4.0"
      }
    }
  }
}
